name: docupass_flutter_example
description: Example app for the DocuPass Flutter SDK.
publish_to: 'none'
version: 0.2.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  docupass_flutter:
    path: ..

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
