name: docupass_flutter
description: Flutter in-app ID verification & KYC SDK for ID Analyzer DocuPass.
version: 0.2.0
homepage: https://github.com/idanalyzer/docupass-flutter
repository: https://github.com/idanalyzer/docupass-flutter

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  camera: ^0.12.0+1
  flutter:
    sdk: flutter
  google_mlkit_face_detection: ^0.13.2
  http: ^1.6.0
  image: ^4.9.1
  permission_handler: ^12.0.3
  webview_flutter: ^4.14.0

dev_dependencies:
  flutter_lints: ^4.0.0
